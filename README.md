# Project Setup

**Installation**

1. Navigate to the `frontend` folder in your terminal.
2. Install project dependencies using npm:

   ```bash
   npm install

3. Run command

   ```bash
   npm run dev
